<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ImagenesMercadoLibre.Views.FilePicker.SingleFolderPage">
    <ContentPage.Content>
        <StackLayout>
            
            <Grid x:Name="LayoutRoot" Margin="12,10,12,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid x:Name="Input" Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="1" x:Name="PickFolderButton" Text="Pick folder" Margin="0,10,10,0"/>
                </Grid>
                <Grid x:Name="Output" Grid.Row="1" >
                    <Label Text="{Binding OutputTextBlock}"
                       HorizontalOptions="Fill"
                       VerticalOptions="CenterAndExpand" /> 
                </Grid>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>