<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Title}"
             x:Class="ImagenesMercadoLibre.Views.MainPage">
    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="{Binding Info}"
               HorizontalOptions="Fill"
               VerticalOptions="CenterAndExpand" />
        <Button Text="Token"
                Clicked="OnTokenButtonClicked" />
        <Button Text="Descargar Articulos"
                Clicked="OnDownloadButtonClicked" />
        <Button Text="Descargar Imágenes"
                Clicked="OnDownloadPicturesButtonClicked" />
        <Button Text="Subir Imágenes"
                Clicked="OnUploadButtonClicked" />
        <Button Text="Subir Todas las Imágenes"
                Clicked="OnUploadAllButtonClicked" />
        <Button Text="Me"
                Clicked="OnMeButtonClicked" />
        <Button Text="Seleccionar Carpeta"
                Clicked="OnSingleFolderPageButtonClicked" />
    </StackLayout>
</ContentPage>